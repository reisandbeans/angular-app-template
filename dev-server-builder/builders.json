{
    "builders": {
        "serve": {
            "implementation": "./dist",
            "schema": "./src/schema.json",
            "description": "Runs client and server in dev mode"
        }
    }
}
